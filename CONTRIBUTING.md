# Contributing

Contributions should preserve CSRForge's local-first and authorized-use model.

Before submitting changes, run:

```bash
npm run typecheck
npm run lint
npm test
npm run build
```

Keep parser and analyzer logic deterministic, focused, and covered by tests.
